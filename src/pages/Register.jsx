import React from "react";
import { Header, RegisterForm } from "../components";

const Register = () => {
  return (
    <div className="bg-customBg2 w-full  h-screen">
      <Header />
      <RegisterForm />
    </div>
  );
};

export default Register;
